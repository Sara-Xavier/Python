#crie uma função que fala se 
#o numero é par ou impar

def par_impar(numero):
    multiplo_de_dois = numero % 2 == 0
    
    if multiplo_de_dois:
        return f"{numero} é par"
    return f"{numero} é ímpar"

numero = int(input("Insira um número"))
print(par_impar(numero))
